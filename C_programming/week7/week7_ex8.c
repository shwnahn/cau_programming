/*
 * 문자열 입력 (fgets)
 */

#include <stdio.h>

int main (void) {
    char str[13];
    
    // 주석을 해제하며 실행해보세요.
    
    // fgets 함수를 이용한 문자열 입력
    // fgets(str, 13, stdin);
    // printf("(fgets)Input string: %s\n", str);
    
    // sizeof 함수를 활용하면?
    // fgets(str, sizeof(str), stdin);
    // printf("(fgets)Input string: %s\n", str);

    return 0;
}
