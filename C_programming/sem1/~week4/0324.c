/* 두 개의 숫자의 합을 계산하는 프로그램 */
#include <stdio.h>

int main(void) {
    int x;      // 첫 번째 값 정수
    int y;      // 두 번째 값 정수
    int sum;    // 두 값의 합

    x = 100;
    y = 200;

    sum = x + y;
    printf("sum is %d\n", sum);

    return 0;
}